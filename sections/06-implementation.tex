% Implementation section for AgriConnect documentation
\section{Implementation}

\subsection{Technology Stack}
\begin{table}[H]
	\centering
	\begin{tabular}{|l|l|}
		\hline
			extbf{Component} & \textbf{Technology} \\
		\hline
		Frontend & Next.js 13, TypeScript, Tailwind CSS \\
		Backend & Django 4, Django REST Framework \\
		Database & PostgreSQL \\
		Authentication & JWT tokens \\
		Deployment & Vercel (frontend), Render (backend) \\
		\hline
	\end{tabular}
	\caption{Technology Stack}
	\label{tab:techstack}
\end{table}

\subsection{Project Structure}
\begin{verbatim}
agri-connect/
|-- frontend/          # Next.js application
|-- backend/           # Django application
|-- docs/              # Documentation
`-- README.md
\end{verbatim}

\subsection{Key Code Examples}
\subsubsection{Django Model Example}
\begin{lstlisting}[language=Python, caption=Farm Model]
from django.db import models

class Farm(models.Model):
	user = models.ForeignKey(User, on_delete=models.CASCADE)
	name = models.CharField(max_length=255)
	location = models.CharField(max_length=500)
	area = models.DecimalField(max_digits=10, decimal_places=2)
	created_at = models.DateTimeField(auto_now_add=True)
    
	def __str__(self):
		return self.name
\end{lstlisting}

\subsubsection{Next.js Component Example}
\begin{lstlisting}[language=JavaScript, caption=Farm Dashboard Component]
import React from 'react';

export default function FarmDashboard() {
	const [farms, setFarms] = React.useState([]);
    
	return (
		<div className="p-4">
			<h1 className="text-2xl font-bold">My Farms</h1>
			{farms.map(farm => (
				<div key={farm.id} className="border p-4 m-2">
					<h2>{farm.name}</h2>
					<p>Location: {farm.location}</p>
				</div>
			))}
		</div>
	);
}
\end{lstlisting}
