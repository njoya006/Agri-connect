# Styles Directory

Use this folder for global design tokens (e.g., CSS variables, theme JSON files, and future SCSS modules). The Tailwind-powered `app/globals.css` file should import any additional artifacts from here as the design system evolves.
